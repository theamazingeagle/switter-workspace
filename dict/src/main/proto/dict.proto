syntax = "proto3";
option java__multiple__files = true;
package com.dict.app;

message request {
    string message = 1;
}

message response {
    string message = 1;
}

service TransformService {
    rpc transform(request) returns (response) {}
}